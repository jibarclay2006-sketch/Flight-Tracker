{
  "name": "flight-tracker-data-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker proxy for the Live Flight Tracker community ADS-B feeds",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
